MESA_BUILDDIR="$HOME/projects/mesa/builddir"

export LD_LIBRARY_PATH="$MESA_BUILDDIR/lib"
export LIBGL_DRIVERS_PATH="$MESA_BUILDDIR/lib"

if [ -z "$MESA_PS1" ]; then
	MESA_PS1=$PS1
fi

PS1="[mesa] $MESA_PS1"
